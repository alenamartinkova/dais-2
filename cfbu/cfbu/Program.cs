using System;
using cfbu.Database;
using cfbu.Database.dao;

namespace cfbu
{
    class Program
    {
        static void Main(string[] args)
        {
            // fachci
            //PlayerTable.PlayerTransfer(1, 6);

            // fachci
            //TeamTable.TeamUpdateCovid(1);

            // fachci
            //TeamTable.TransferTeam(2, 558);

            // fachci
            //DateTime date = new DateTime(2021, 6, 11);
            //TeamMatchTable.UpdateMatchDate(5, date);

            // fachci
            //TicketTable.CreateTicket(5, "test", "test", "test", 123);
        }
    }
}
